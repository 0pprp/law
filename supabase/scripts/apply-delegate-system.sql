-- ⚠️ لا تشغّل هذا الملف كاملاً دفعة واحدة — سيظهر خطأ 55P04
--
-- شغّل بالترتيب في جلستين منفصلتين في Supabase SQL Editor:
--
-- 1) apply-delegate-system-step1.sql   ← إضافة قيمة enum فقط
-- 2) apply-delegate-system-step2.sql   ← الجداول والسياسات
--
-- إذا نفّذت الخطوة 1 سابقاً ونجحت، شغّل step2 فقط الآن.

SELECT 'استخدم apply-delegate-system-step1.sql ثم apply-delegate-system-step2.sql' AS instruction;
